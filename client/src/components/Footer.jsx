const styles = {
  container: {
    display: "flex",
    flexDirection: "row",
    justifyContent: "center",
    alignItems: "center",
  },
};

export default function Footer() {
  return (
    <div style={styles.container}>
      <h5>Follow this link for step-by-step tutorial!</h5>
    </div>
  );
}
